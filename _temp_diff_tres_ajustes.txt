diff --git a/public/css/forms.css b/public/css/forms.css
index a0990cb..0fc7f6b 100644
--- a/public/css/forms.css
+++ b/public/css/forms.css
@@ -90,6 +90,66 @@ input[type="checkbox"]:focus-visible {
   outline-offset: 2px;
 }
 
+.stepper-label {
+  font-family: var(--font-ui);
+  font-size: 11px;
+  font-weight: 500;
+  letter-spacing: 0.06em;
+  text-transform: uppercase;
+  color: var(--dark-muted-text);
+  display: block;
+  margin-bottom: 6px;
+}
+
+/* Stepper de "Nombre de places": substitueix l'input numèric pla. El valor
+   real es manté a l'input ocult #cantidad (llegit tal qual per checkout.js
+   en construir el body de la compra), perquè els botons +/- no dupliquin
+   cap lògica de recàlcul: només canvien aquest input i en disparen l'event
+   "input" ja escoltat avui (recalcula preu i acordeó d'acompanyants). */
+.stepper-cantidad {
+  display: flex;
+  align-items: center;
+  gap: 16px;
+  width: fit-content;
+  margin-bottom: 16px;
+}
+
+.stepper-btn {
+  flex: 0 0 auto;
+  width: 40px;
+  height: 40px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  border: none;
+  border-radius: 50%;
+  background: var(--dark);
+  color: var(--white);
+  font-family: var(--font-ui);
+  font-size: 18px;
+  line-height: 1;
+  cursor: pointer;
+  transition: background 0.2s ease, opacity 0.2s ease;
+}
+
+.stepper-btn:hover:not(:disabled) {
+  background: var(--dark-hover);
+}
+
+.stepper-btn:disabled {
+  opacity: 0.35;
+  cursor: not-allowed;
+}
+
+.stepper-valor {
+  min-width: 28px;
+  text-align: center;
+  font-family: var(--font-ui);
+  font-size: 16px;
+  font-weight: 500;
+  color: var(--dark);
+}
+
 /* Prefix de país + número de telèfon com un sol camp visual. El prefix és
    un desplegable personalitzat (bandera + país + codi) en lloc d'un
    <select> natiu, perquè el navegador no permet estilitzar la llista
diff --git a/public/css/layout.css b/public/css/layout.css
index 4414f3b..f796c2e 100644
--- a/public/css/layout.css
+++ b/public/css/layout.css
@@ -32,58 +32,35 @@
   position: relative;
 }
 
-/* Ancoratge del crumb de tornada: perquè quedi sempre al mateix nivell que
-   el títol de l'esdeveniment (i no un valor fix en píxels, fràgil si canvia
-   l'alçada del text), s'ubica dins el mateix bloc que el títol i es
-   posiciona relatiu a aquest contenidor, no al panell sencer. */
-.evento-info-titol {
-  position: relative;
-}
-
 /* Crumb de tornada al selector d'esdeveniments (només visible quan s'hi ha
-   arribat des de la quadrícula, quan hi havia diversos esdeveniments actius). */
+   arribat des de la quadrícula, quan hi havia diversos esdeveniments actius).
+   Viu FORA de .card, com una línia pròpia per sobre de la targeta: amb
+   noms d'esdeveniment llargs el títol (h1) pot ocupar tota l'amplada del
+   panell fosc i arribar a dues línies, de manera que un crumb ancorat en
+   absolut a la cantonada del panell (com abans) acabava solapant-se amb el
+   text del títol. Fora de la targeta mai hi pot xocar, en cap amplada. */
 .breadcrumb-tornar {
-  position: absolute;
-  top: 0;
-  right: 0;
-  /* Sense això l'eyebrow (bloc a tota l'amplada, mateix requadre) rebia
-     els clics per sobre del botó tot i ser contingut estàtic. */
-  z-index: 1;
+  display: block;
+  width: 100%;
+  max-width: 920px;
+  margin: 0 auto 12px;
+  padding: 0 40px;
   background: transparent;
   border: none;
-  padding: 0;
   font-family: var(--font-ui);
   font-size: 11px;
   font-weight: 500;
   letter-spacing: 0.03em;
-  color: var(--light-muted);
+  color: var(--dark-muted-text);
   cursor: pointer;
   transition: color 0.2s ease;
 }
 
 .breadcrumb-tornar:hover {
-  color: var(--white);
+  color: var(--dark);
   text-decoration: underline;
 }
 
-/* Només a escriptori: el crumb puja al mateix nivell que el títol.
-   Traiem el position:relative de .evento-info-titol perquè el contenidor
-   de referència de l'absolute torni a ser .panel-color (que ja té
-   position:relative). Com que top/right d'un absolute es calculen contra
-   la padding-box del seu contenidor —no contra on comença el contingut—,
-   cal repetir el padding del panell (48px 40px) perquè quedi alineat amb
-   el títol en lloc de la cantonada exterior del panell. A mòbil es manté
-   el comportament original (relatiu a .evento-info-titol). */
-@media (min-width: 721px) {
-  .evento-info-titol {
-    position: static;
-  }
-  .breadcrumb-tornar {
-    top: 48px;
-    right: 40px;
-  }
-}
-
 .panel-white {
   background: var(--light);
   justify-content: center;
@@ -249,6 +226,9 @@
   .panel {
     padding: 32px 24px;
   }
+  .breadcrumb-tornar {
+    padding: 0 24px;
+  }
 }
 
 /* Selector d'esdeveniment: quan hi ha diversos esdeveniments actius alhora,
diff --git a/public/css/style.css b/public/css/style.css
index 6328e9e..57c3962 100644
--- a/public/css/style.css
+++ b/public/css/style.css
@@ -2,5 +2,5 @@
    No afegir regles directament aquí; veure /public/css/*.css */
 @import url('variables.css?v=4');
 @import url('legal-modal.css?v=2');
-@import url('layout.css?v=20');
-@import url('forms.css?v=24');
+@import url('layout.css?v=21');
+@import url('forms.css?v=25');
diff --git a/public/index.html b/public/index.html
index 3894dcc..b701200 100644
--- a/public/index.html
+++ b/public/index.html
@@ -10,7 +10,7 @@
 <!-- Inter via Google Fonts (Ogg és una tipografia amb llicència pròpia de UAUU:
      veure comentari a css/style.css sobre com afegir-la a public/fonts/) -->
 <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
-<link rel="stylesheet" href="/css/style.css?v=15">
+<link rel="stylesheet" href="/css/style.css?v=16">
 </head>
 <body class="pagina-principal">
 
@@ -31,11 +31,12 @@
   <div class="selector-grid" id="selector-grid"></div>
 </section>
 
+<button type="button" class="breadcrumb-tornar hidden" id="btn-tornar-selector">‹ Tots els esdeveniments</button>
+
 <main class="card hidden" data-layout="info-left" id="main-card">
 
   <section class="panel panel-color evento-info">
     <div class="evento-info-titol">
-      <button type="button" class="breadcrumb-tornar hidden" id="btn-tornar-selector">‹ Tots els esdeveniments</button>
       <h1 id="evento-nombre">Carregant l'esdeveniment…</h1>
       <p class="evento-data" id="evento-data"></p>
       <p id="evento-descripcio"></p>
@@ -45,7 +46,7 @@
       <div id="evento-convidats-llista"></div>
     </div>
     <div class="evento-meta">
-      <span class="eyebrow">Preu</span>
+      <span class="eyebrow">Preu del menú</span>
       <span class="evento-preu" id="evento-preu"></span>
     </div>
   </section>
@@ -57,8 +58,13 @@
     <div id="estat-no-disponible" class="estat-missatge hidden" role="status" aria-live="polite"></div>
 
     <form id="form-compra">
-      <label for="cantidad">Nombre de places</label>
-      <input type="number" id="cantidad" name="cantidad" min="1" value="1" required>
+      <span class="stepper-label" id="cantidad-label">Nombre de places</span>
+      <div class="stepper-cantidad" role="group" aria-labelledby="cantidad-label">
+        <button type="button" class="stepper-btn" id="btn-cantidad-menys" aria-label="Reduir nombre de places">−</button>
+        <span class="stepper-valor" id="cantidad-valor" aria-live="polite">1</span>
+        <button type="button" class="stepper-btn" id="btn-cantidad-mes" aria-label="Augmentar nombre de places">+</button>
+      </div>
+      <input type="hidden" id="cantidad" name="cantidad" value="1">
 
       <label for="nombre_comprador">Nom i cognoms</label>
       <input type="text" id="nombre_comprador" name="nombre_comprador" required>
@@ -120,7 +126,7 @@
   </div>
 </div>
 
-<script src="/js/checkout.js?v=30"></script>
+<script src="/js/checkout.js?v=31"></script>
 <script src="/js/legal-modal.js?v=2"></script>
 </body>
 </html>
diff --git a/public/js/checkout.js b/public/js/checkout.js
index 0825493..9584c7d 100644
--- a/public/js/checkout.js
+++ b/public/js/checkout.js
@@ -462,6 +462,26 @@ function actualitzarAcompanyants() {
   renderAcompanyants(Math.max(0, cantidad - 1));
 }
 
+// Stepper de "Nombre de places": +/- només toquen l'input #cantidad (el
+// que ja es llegia a mà abans) i en disparen l'event "input", perquè el
+// recàlcul de preu i acordeó d'acompanyants segueixi passant pel listener
+// existent (dispatchEvent) en lloc de duplicar-lo aquí.
+function actualitzarEstatStepperCantidad() {
+  const cantidad = parseInt(document.getElementById('cantidad').value, 10) || 1;
+  document.getElementById('cantidad-valor').textContent = cantidad;
+  document.getElementById('btn-cantidad-menys').disabled = cantidad <= 1;
+}
+
+function canviarCantidad(delta) {
+  const input = document.getElementById('cantidad');
+  const actual = parseInt(input.value, 10) || 1;
+  const nova = Math.max(1, actual + delta);
+  if (nova === actual) return;
+  input.value = nova;
+  actualitzarEstatStepperCantidad();
+  input.dispatchEvent(new Event('input'));
+}
+
 // Preu de la fitxa: es repinta amb el preu unitari de l'evento (precioUnitari)
 // multiplicat per "Nombre de places". El preu real cobrat es calcula al
 // backend (crearCheckoutSession); això és només reflex visual.
@@ -690,5 +710,8 @@ document.addEventListener('DOMContentLoaded', () => {
     actualitzarAcompanyants();
     actualitzarPreu();
   });
+  document.getElementById('btn-cantidad-menys').addEventListener('click', () => canviarCantidad(-1));
+  document.getElementById('btn-cantidad-mes').addEventListener('click', () => canviarCantidad(1));
+  actualitzarEstatStepperCantidad();
   actualitzarAcompanyants();
 });
